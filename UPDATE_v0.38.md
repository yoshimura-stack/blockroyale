# v0.38 RESULT & LANDING FIX

## 更新方法
v0.37へこのZIPの中身を上書きして公開してください。追加SQLは不要です。公開後はPLAYERを更新してください。

## 結果表示
添付の2画面はどちらも同じ優勝者を表示しており、敗者は順位2・K.O.でした。全員にWINNERを大きく表示する既存の実装が誤解を招いていました。

- 優勝者：WINNER、自分の名前・スコア、「あなたが優勝」。
- 敗者：GAME OVER、自分の名前・スコア・順位、別の行に優勝者名。
- 状態欄は取得した結果の勝者IDで決定し、ローカルのaliveだけでWINNERと表示しません。
- 勝敗ルール・順位決定SQL・PROJECTORの共通優勝者発表は変更していません。

## 邪魔ブロック投下通知
投下用landingクラスがトップページ用landingクラスと衝突し、min-height:100vhなどが適用されていました。投下専用のgarbage-landingへ分離しました。

- 盤面の上寄りにコンパクトなバナーを表示。
- 背景は不透明度42%、背後をぼかさずミノが見えるように変更。
- 表示は約0.95秒。操作を遮りません。
- 「あと何ターン」の予告・攻撃ルール・v0.36のリセット・v0.37の背景は維持。

## 確認
クライアントのモックテスト13項目とJS構文チェックに合格。敗者のスコアが勝者より高い場合や、ローカルaliveが古い場合も表示を検証しました。ブラウザではローカルのテスト結果を使ってGAME OVER表示と投下バナーを確認しました（バナー88px、背景rgbaのalpha 0.42、blurなし）。本番DBでの2端末対戦は未実施です。
