# v0.39 RC1 検証記録

実施日：2026-09-17。本番Supabaseへ変更を加えず、ローカルで検証。

## 自動検証

- DBテスト9項目：権限・NULL認証の拒否、参加上限50、READY再送、HOST表示による非破壊、開始予告、攻撃重複・宛先認可、K.O.順位と唯一の勝者、NEXT/RESET後の旧通信拒否、切断失格・全員切断、旧領域の保持。
- クライアントテスト9項目：HOST初期表示、開始通知取りこぼしの復旧、試合中リロード失格、本人別の結果表示、攻撃重複と世代切替、半透明投下通知、同一パケット再送と新しいACKの保持、通信非重複、非表示タブの時間進行と次戦スコア初期化。
- Tetrisエンジン・Canvas描画・背景演出JSはv0.38とバイト単位で同一。
- 検証対象のJS全ファイルの構文確認。

## ブラウザ検証

実ブラウザ＋ローカルPGliteアダプターで2人が入室し、HOSTから開始。1人を試合中に再読み込み・再接続させて失格にし、敗者のGAME OVERと本人名/順位、PROJECTORの同一勝者表示を確認。HOST再読み込みでRESULTと参加者を維持。NEXTで接続中PLAYERがREADYへ戻ることを確認。

1280×720で、左右パネル・入室フォーム・盤面が表示されることを目視確認。フルHDでの実描画は未検証（ビューポート指定が反映されなかったため、確認済みとは扱わない）。実機テストで各PCのサイズも確認してください。

## 50模擬クライアントのRPC試験

60秒間、50クライアントとHOST/PROJECTOR相当の要求をHTTPでローカルアダプターへ送信。攻撃を発生させ、最後に49人のK.O.をまとめて送り、勝者1人・50通りの順位を確認。

| 指標 | ローカル実測 |
|---|---:|
| 総リクエスト数（準備・片付け込み） | 3,099 |
| 通信再試行 | 0 |
| サーバーが受理した攻撃 | 50 |
| 応答時間p95 | 約32ms |
| 最大応答時間 | 約220ms |
| 応答本文の合計 | 約5.97MB |
| 応答本文からの1時間換算 | 約0.33GB |

**この数字はSupabase Freeの処理能力を示しません。** PGliteはDB処理を直列実行するため、実際のPostgreSQLの同時接続・ロック待ち・デッドロック性能は検証できていません。50個のブラウザでゲームを操作したテストでもありません。インターネット、社内Wi-Fi、端末性能、実Auth、PostgRESTのキャッシュ/権限、Freeの制約は段階的な実環境テストで確認が必要です。

## セキュリティ上の確認と範囲

- br39の表・補助関数は公開ロールにアクセスを許可せず、RLSを有効化。
- SECURITY DEFINERの公開入口のみ、空のsearch_pathと明示的権限を設定。HOSTはauth.uid()の許可リストを毎回確認。
- PLAYERはランダムな個別トークンのハッシュで識別。公開APIキーだけで他人の状態や主催者操作を変更できないことを確認。
- 表示名はtextContentで出力。スコア・盤面自体の不正申告を検出する仕組みは未実装。
- 旧版の公開テーブル・RPC権限は維持。新版で旧版のセキュリティも改善したとは主張しない。

## 再現方法

同梱testsフォルダで `npm install`、`npm test`。Node.jsとPGliteが必要です。これらの自動試験はローカルだけを使用します。実Supabaseの負荷試験はtools/LOAD_TEST.mdの明示的な操作で実行します。
